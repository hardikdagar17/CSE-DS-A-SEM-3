function multiply(a,b) {
    return a*b;
}
function calculate(a,b) {
    let num1 = prompt("Enter first no. : ");
    let num2 = prompt("Enter second nunber : ");
    num1=Number(num1);
    num2=Number(num2);
    let 
}